# Launch program — decisions log

Append-only. Root `DECISIONS.md` holds the Phase 1/2 engineering log; this file holds launch-program decisions. Every entry: decision, reason, alternatives, risks, date, related refs.

| # | Date | Decision | Reason | Alternatives considered | Risks | Refs |
|---|---|---|---|---|---|---|
| L1 | 2026-07-18 | Working copy is `C:\Users\Harry\OneDrive\Documents\Site-Audit-663` (holds the audit artifacts; operator brief targets it). The `Downloads\prymd\audit-snap-663` clone is the historical Phase 1/2 workspace; `GitHub\audit-snap-663` clone is untouched. | One canonical workspace prevents split-brain edits. | Working in prymd clone (stale, wrong artifacts location). | OneDrive sync could interfere with node_modules/metro; watch for it. | Operator brief §Repository safety |
| L2 | 2026-07-18 | Launch branch `claude/punchthis-launch` created from `origin/main` @ `d90a38d` (exactly the commit the review audited). Old local branch `hardening/tests-performance` left untouched. | Brief requires implementation from latest main on a dedicated branch; audited commit = known-good baseline. | Worktree isolation (unnecessary — tree was clean). | None material. | git reflog |
| L3 | 2026-07-18 | Audit review MD committed to the repo; `.audit-output/` added to `.gitignore` (binaries stay local, referenced by relative path). | Review is the program's source document and must survive clones; 23 PNGs would bloat history. | Committing screenshots too; keeping both untracked. | Image links in the review 404 in other clones — acceptable, noted here. | LP docs |
| L4 | 2026-07-18 | Task-ID scheme `LP-nn`, mapped to operator P0.x sections in the master plan. Root ROADMAP's A/B numbering is closed history and stays frozen. | Two live numbering schemes would collide. | Reusing A-numbering. | None. | LAUNCH_MASTER_PLAN.md |
| L5 | 2026-07-18 | First implementation task is **LP-01 (true Clear All Data)**, not P0.1 identity. | Deletion dishonesty is the top live data-integrity/trust defect and is fully unblocked; identity (LP-08) is gated on name clearance (EXT-1) + store accounts (EXT-3), and web-PWA Gate A does not need it. | Identity-first (brief lists it first); demo-data-first (smaller but lower risk). | None — identity starts the moment EXT-1/EXT-3 clear. | Review §11, brief P0.2 |
| L6 | 2026-07-18 | Cursor is driven via ready-to-paste prompts; Claude verifies each result directly. No headless Cursor CLI (`cursor-agent`) exists on this machine — only the editor launcher (`cursor` 3.12.17). | Brief mandates programmatic invocation only "if available". | Installing cursor-agent CLI (needs operator; can be adopted later). | Loop latency depends on operator pasting tasks. | tool check 2026-07-18 |
| L7 | 2026-07-18 | Gate A ships as **controlled web/PWA early access on the existing Render deploy** (invite-only, founder-supported); TestFlight/internal-Android become Gate A add-ons only after EXT-3 + LP-08. | All Gate A required conditions are achievable on web/PWA now; store distribution is blocked externally. | Waiting for TestFlight (blocks Gate A on Apple account for no safety gain). | PWA/iOS-Safari storage eviction risk — mitigated by LP-05 export + honest warning (LP-04). | Master plan §Gates |
| L8 | 2026-07-18 | Gate A accessibility bar = shared primitives + roles/labels/states + WCAG AA contrast + web keyboard journey (LP-06). Full native VoiceOver/TalkBack pass rides LP-09 (devices). | No physical devices available to the program yet (EXT-5). | Blocking Gate A on native SR testing (would stall the gate on hardware). | Native SR defects reach early-access users; mitigated by small invited cohort + founder support. | Review §Accessibility |
| L9 | 2026-07-18 | P0.5 legal wording ships in Gate A verbatim from the operator's drafts, visibly labelled as provisional pending legal review (EXT-4). | Honest wording now beats no wording; drafts are operator-approved. | Waiting for counsel (stalls Gate A). | Wording may change post-review; screens must make copy easy to swap (single module). | Brief P0.5 |
| L10 | 2026-07-18 | **Global-first product identity (operator EXT-1).** PunchThis is a global product with Australia as initial base/storefront. Preferred bundle/package ID `com.punchthis.app` (subject to domain + clearance), **no country code in identifiers**. App must not hardcode AU assumptions: device-locale date/time formatting, internationally flexible addresses, no mandatory AU-only fields, metric+imperial when measurements arrive, timezone kept with stored dates, copy externalised for future i18n, regional store pricing (never hardcoded conversions). Identity NOT locked until LP-10 clearance search + domain check complete. | Operator decision. | AU-scoped identity (`au.com.*`). | `com.punchthis.*` may be taken → revisit on LP-10 findings. | LP-03, LP-08, LP-10 |
| L11 | 2026-07-18 | **Public legal routes (operator EXT-2):** four small independent static routes — `/privacy`, `/terms`, `/support`, `/data-deletion` — under the eventual product domain or a stable static host. Mobile-friendly, versioned, public, provisional-marked; privacy describes actual behaviour only; support names the real publisher; deletion page describes Clear All Data accurately; **no account-deletion process described (no accounts exist)**. Blocked only for store submission — not for Gate A. No marketing-site work. | Operator decision. | Pages on the unfinished marketing site (rejected). | Placeholder-domain churn if name changes → deploy after LP-10. | LP-12, RELEASE_CHECKLIST Gate B |
| L12 | 2026-07-18 | **Store accounts (operator EXT-3):** status unconfirmed. Web/PWA Gate A proceeds; TestFlight/internal-Android/signing/store submission stay blocked until accounts exist. Claude prepares an owner checklist (LP-11) of required identity/assets. Publisher must be the real AU legal entity or individual — never invented. | Operator decision. | — | — | LP-11 |
| L13 | 2026-07-18 | **Legal review (operator EXT-4):** provisional plain-language wording ships for Gate A (no certification/secure-backup/guaranteed-retention claims; documentation-tool scope; local-until-sync; user responsible for reviewing/distributing reports). Formal review of privacy/terms/disclaimer/retention/deletion/governing-law/international wording required before Gate B; AU governing law; global availability must not imply per-country compliance certification. LP-01…07 do not wait. | Operator decision. | — | — | LP-04, Gate B checklist |
| L14 | 2026-07-18 | **Device testing (operator EXT-5):** proceed with web + responsive + iOS simulator/Android emulator where available + automated checks. Physical-device matrix (list fixed in checklist/QA_MATRIX) required before any native public launch; Claude prepares builds + QA scripts (LP-13) so the owner/tester can run it efficiently. Only native release approval stays blocked. | Operator decision. | Blocking all work on devices (rejected). | Native-only defects surface late; mitigated by simulator passes + LP-13 scripts. | LP-09, LP-13, QA_MATRIX |
| L15 | 2026-07-18 | **Pricing (operator EXT-6) — hypotheses, not commitments:** pilot (first 10–20 users) free 30–60 days with founder support + structured feedback; measure completed audits, reports, repeat usage, time saved, WTP. Then Founding Inspector ≈ **A$129 one-time** (range A$99–149; global anchor ≈ US$79–99 via regional store pricing): unlimited local projects/reports, branding, CSV, all presets, export/archive, 12 months updates, **no lifetime-cloud promise**; cloud/team sold separately. Later (only after dependable backup/sync): Solo Pro ≈ US$14.99/mo / US$149/yr (AU ≈ A$22.99/A$229); Team ≈ US$49/mo incl. 3 internal seats, free contractor participation, extra seats US$10–15. **No paywall built now; pricing never blocks Gate A.** | Operator decision; market anchors: Site Audit Pro £12.90 one-time + cloud subs, SafetyCulture US$24/seat/mo, Fieldwire US$39/user/mo, PlanRadar ~A$42/user/mo. | Immediate paid launch (rejected — validate WTP first). | Anchors drift — recheck official pricing pages before public comparisons. | Master plan §Monetisation, entitlements stub |
