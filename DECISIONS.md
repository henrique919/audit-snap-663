# DECISIONS.md — Architectural & Process Decisions Log

Append-only. Each entry: date, decision, one-line rationale. Phase 2 (Sonnet 5) MUST
append here whenever it resolves an ambiguity on its own, per EXECUTION_PLAYBOOK.md.

| # | Date | Decision | Rationale |
|---|------|----------|-----------|
| 1 | 2026-07-12 | Workspace is a full clone at `C:\Users\Harry\Downloads\prymd\audit-snap-663`; all Phase 1/2 work happens here. | Session scratchpad is volatile; this path survives session/model switches. |
| 2 | 2026-07-12 | Phase 1/2 commits are made locally on `main`; no pushes to origin unless the operator explicitly asks. | Pushing is an outward-facing action; local commits satisfy the handoff requirement. |
| 3 | 2026-07-12 | The unmerged remote commit `20d5927` ("Preserve pending writes across retry failures", branch `hardening/data-durability`) is treated as roadmap item A1, to be cherry-picked and verified — not silently merged during Phase 1. | Phase 1 is review-only; the commit changes app logic and must go through the item loop with testing. |
| 4 | 2026-07-12 | Web preview is used as the Phase 2 verification surface, with known web-platform limitations documented in STATUS.md §5 (no file persistence, no real image pipeline, no PDF file output on web). | It is the only runtime available to an autonomous agent on this machine; native-only behaviors are verified by unit tests instead. |
| 5 | 2026-07-12 | Package manager: bun (bun.lock is authoritative; package-lock.json also exists from Wave 1 CI). Use `bun install` + `npx` for tools. | Matches repo lockfile and existing scripts. |
| 6 | 2026-07-12 | Branding is already centralized in `expo/constants/config.ts` (BrandConfig); Phase 1 adds no new branding file. `app.json` cannot import TS, so rename remains a two-file change (config.ts + app.json) documented in ARCHITECTURE.md. | Guardrail: avoid rewriting working code; app.json is a static manifest by design. |
| 7 | 2026-07-12 | A1 (cross-platform dialog shim) is ordered before all other roadmap items, ahead of the PDF fix. | `Alert.alert` is a no-op on web; until it is replaced, Phase 2 cannot see its own failures or drive confirm flows in the verification surface. |
| 8 | 2026-07-12 | Web preview automation specifics (CSS-pixel coords, testID tap helper, stepped pointer events with buttons=1 for drawing, localStorage assertions, deep links, file-input injection) are codified in EXECUTION_PLAYBOOK.md §4 as binding technique. | Phase 1 empirically found naive clicks/drags fail against RN-web; without these techniques Phase 2 would misdiagnose working features as broken. |
| 9 | 2026-07-12 | The dev server config lives OUTSIDE the repo at `C:\Users\Harry\Downloads\prymd\.claude\launch.json` (entry `clean-audit-iq-web`, port 8091). | The Browser-pane preview tool reads launch.json from the session working directory, not the repo. |
| 10 | 2026-07-12 | A2 web export: DO record a `ReportExport` row on web, using the sentinel `pdfUri: "web-print"` (exported as `WEB_PRINT_SENTINEL` from `lib/reportPrintWeb.ts`), rather than skipping the record entirely. `Share`/`Email` on web always regenerate (bypass `ensurePdf`'s reuse/staleness logic, which is native-only via its existing `Platform.OS !== "web"` guard) instead of trying to reuse the sentinel as if it were a real file reference. | Keeps `reportState` ("ready"/"stale"/"none"), the freshness-warning banner, and the Reports-tab history list (A9) all functioning coherently on web without inventing a second status model. Confirmed `expo-print`'s web shim (`ExponentPrint.web.ts`) ignores the `html` option and just calls `window.print()` on the current page — it cannot render report HTML at all — so a separate window opened synchronously (before any `await`, to survive popup blockers) and populated via `document.write` is the only viable web path; this is the same "use your browser's print dialog" behavior the app already told users to expect. |
| 11 | 2026-07-12 | Popup-blocked is treated as an expected, first-class failure mode (not an edge case): `generate()` checks `openBlankPrintWindow()`'s result before doing any work and shows `showAlert("Pop-up blocked", …)` with zero side effects (no `ReportExport` row) if refused. | Verified empirically in the web preview: browsers require a *trusted* user gesture to allow `window.open()`, and neither this project's own synthetic `dispatchEvent`-based test-tap helper (EXECUTION_PLAYBOOK §4.2) nor the Browser-pane automation's clicks reliably produce one — real users clicking directly will not hit this in practice, but any automated re-verification of this flow will, so the fallback had to be correct and non-destructive by construction, not just by manual spot-check. |
| 12 | 2026-07-12 | A4 save-state logic extracted into pure `expo/lib/saveState.ts` (`saveIndicatorState`, `savedToastMessage`) rather than inlined per-screen or fixed via a Settings dev-toggle. Verified correctness through unit tests (11 cases) plus live instrumentation of `persistStatus`/`markPersistFailure`/`persistFailureVersion`, NOT through a stable live screenshot of the error state (see #13 — the live environment can't hold "error" still long enough to screenshot). | ROADMAP explicitly allowed "a unit test at minimum" in place of a dev-only UI toggle; the pure-function extraction is required because screens live outside `lib/` (the jest `roots`) and a toggle would add permanent app-facing surface for a Phase-2-only verification need. |
| 13 | 2026-07-12 | **Environment artifact, not a product bug**: in this headless Browser-pane, `react-native-web`'s `AppState` polyfill oscillates `background⇄active` roughly every ~200ms (confirmed via direct instrumentation — 14 cycles in 3 seconds). `AppStore.tsx`'s `AppState` listener flushes on every `active→background` edge (correct, intentional design — flush before the app is backgrounded), so this churn keeps re-triggering `flushPersist()` on an already-failing write, meaning `persistStatus` cycles `saving→error→saving→…` far faster than any check/screenshot can catch a stable "error" frame — "saving" is always the freshest render by the time anything reads it, even though `markPersistFailure` (confirmed via `persistFailureVersion` incrementing and `lastPersistError` populating correctly) genuinely fires. Do not mistake this for a real bug on future items — a real device/browser has stable focus/visibility signals and would not exhibit this churn. If a future item needs to observe a *settled* `persistStatus === "error"`, prefer the direct `window.__liveStatus`-style ground-truth global technique over polling the DOM, and expect it to require checking on the FIRST failure within roughly ~1 second of triggering it, before AppState churn requeues another attempt. | Cost ~45 minutes of live debugging in A4 before being isolated; documented so it isn't re-litigated in A5–A12 or the Final QA loop, which will hit the same environment behavior any time they force a persistence failure. |
| 14 | 2026-07-12 | Phase 2 continues in a fresh remote container (no Windows machine, no Browser-pane tool). Web preview is driven via a hand-rolled Playwright harness (`node` + the globally installed `playwright` package pointed at the pre-provisioned `/opt/pw-browsers/chromium`) scripted against `npx expo start --web --port 8091`, run with `EXPO_OFFLINE=1 CI=1 EXPO_NO_TELEMETRY=1` to skip Expo's doctor/telemetry network calls (they hang against this environment's proxy otherwise). All EXECUTION_PLAYBOOK.md §4 techniques (testID tap helper, localStorage assertions, file-input injection, fake-`window.open` capture) carry over unchanged against this harness. | The playbook's tooling references (`preview_start`, `C:\Users\Harry\...\launch.json`) are specific to the prior machine/session and don't exist here; the underlying app and verification *technique* are unaffected, only the driver mechanics. |
| 15 | 2026-07-12 | **Binding technique correction to EXECUTION_PLAYBOOK.md §4**: do NOT run `git stash` / `git stash pop` while the Expo web dev server is running against the same working tree. Confirmed by direct repro during A9: after a stash/pop cycle, Metro's file watcher silently stopped picking up further edits to the touched file (`app/(tabs)/reports.tsx`) — it kept serving a bundle frozen at a stale version of the file's contents (verified by `curl`-ing the raw `entry.bundle` and finding code that had since been edited out) while `tsc`, `git diff`, and disk contents all showed the new code correctly. This produced ~40 minutes of misleading test results that looked exactly like a dead click handler (real Playwright clicks, direct `props.onClick` invocation, and `history.pushState` spying all showed nothing happening) but were actually just exercising old JS. Root cause isolated by comparing the served bundle's source against the file on disk, not by inspecting the running app. Fix: if a stash/pop (or any out-of-band write to a watched file) happens while Metro is up, kill the dev server fully and restart with `--clear`, then re-verify from a clean bundle before trusting any subsequent click-through test. | Future items must not lose time to the same false trail — if a testID tap/click "does nothing" with no console error, no thrown exception, and no `history.pushState` call, suspect a stale Metro bundle (check via `curl .../entry.bundle | grep <a string unique to your latest edit>`) before suspecting Pressability/React internals. |
| 16 | 2026-07-12 | **Generalizing #15**: the Metro stale-bundle issue recurred in A10 with no `git stash` involved at all — three separate, unremarkable file edits (`preview.tsx`, `hitlist.tsx`, `issue/[id].tsx`) simply never made it into the served bundle. This means the file watcher in this container is unreliable in general (plausibly inotify/filesystem-driver related to the sandboxed environment), not specifically a git-stash interaction. **New binding technique for the remainder of Phase 2**: before trusting ANY click-through/behavioral test result, first `curl` the served `entry.bundle?platform=web&dev=true` and `grep` for one exact string unique to the just-made edit; only proceed with Playwright verification once that string is confirmed present. If absent, kill the dev server and restart with `--clear` before re-testing — do not add delays/retries hoping the watcher catches up, it doesn't self-heal. | A second unrelated occurrence in the very next item confirms this is an environment property to route around every time, not a one-off fluke tied to git plumbing — codifying the check as a mandatory pre-test step (rather than a thing to remember only after being burned) is cheaper than repeating the ~40 min debugging loop from #15 in A11/A12/Final QA. |
| 17 | 2026-07-12 | **Binding technique correction/addition to EXECUTION_PLAYBOOK.md §4.5**: the documented raw-DOM `<input>` file-injection technique (set `.files` + dispatch a `change` event directly) does not reliably work against `expo-image-picker`'s web implementation in this remote environment's headless Chromium — its picker dispatches an *untrusted* synthetic `MouseEvent('click')` on the hidden input (not a real `.click()` call), and headless Chromium auto-cancels that almost immediately (confirmed via `MutationObserver`: the input is added and removed within one poll tick), racing and sometimes crashing the library's own cancel-handling code with a `removeChild`-on-detached-node error if raw injection is attempted mid-race. **Use Playwright's native `page.waitForEvent('filechooser')` + `chooser.setFiles([...])` API instead** for any A11/A12/Final-QA flow that opens `expo-image-picker`'s gallery/camera picker on web — arm the `waitForEvent('filechooser')` promise *before* the triggering click (not after), since the event can fire synchronously with the click. This intercepts Chromium's native CDP-level file-chooser-open request directly and worked cleanly on the first attempt with zero collateral errors. Confirmed the underlying app code was never at fault in the case that surfaced this (A11) — the click handler, `ImagePicker.launchImageLibraryAsync` call, and downstream processing all fired correctly once real files were supplied; only the raw-DOM-injection *test technique* was incompatible with this library + this headless browser combination. | This is the second and third time (see #15, #16) that a test-technique/environment mismatch has cost significant debugging time before being correctly attributed away from the app code; recording the concrete working alternative (not just "be suspicious") saves that cost on every remaining item that touches the photo picker (A12 does not, but Final QA's flow checklist step 3 does). |
| 18 | 2026-07-12 | **Root cause of #15/#16, found during A12**: the dev server was being started with `CI=1` in its environment (added early in Phase 2 purely to skip Expo's doctor/telemetry network calls, which hang against this container's proxy). Metro prints, and honours, `"Metro is running in CI mode, reloads are disabled. Remove CI=true to enable watch mode."` — **`CI=1` disables the file watcher entirely**, which is the actual mechanism behind every "stale bundle" incident in #15 and #16, not an environment-specific inotify flakiness as originally guessed. Fix: start the server as `EXPO_OFFLINE=1 EXPO_NO_TELEMETRY=1 npx expo start --web --port 8091` (no `CI`) — confirmed this still skips the network-dependent doctor check (no hang, no "Host not i..." JSON-parse error) while restoring correct live-reload: a real edit (append then remove a marker comment in `lib/entitlements.ts`) was picked up by the served bundle within 3s each way, with no server restart. **All servers for the remainder of Phase 2 (rest of A12, Final QA) are started this way.** The `curl`-the-bundle-and-grep freshness check from #16 is downgraded from mandatory-every-time to a spot-check — cheap insurance, not a load-bearing workaround anymore. | Saved re-deriving this for Final QA, which touches every screen and would have re-paid the ~40+min stale-bundle debugging tax repeatedly at the worst possible time (the final sign-off sweep). Superseding rather than deleting #15–#17: their *symptom description* and the filechooser-API technique in #17 remain independently correct and useful; only the "why" for the staleness itself was wrong. |
| 19 | 2026-07-12 | **Real defect found and fixed in the Final QA Loop, Step 5 (Markup)**: `app/markup/[assetId].tsx`'s `elements` React state AND its `elementsRef` mirror both initialized as `useState(existing?.elements ?? [])` / `useRef(existing?.elements ?? [])` — a **one-time snapshot** taken at first render, with zero `useEffect` anywhere in the file to re-derive them if `existing` (sourced from `db.annotations`, itself populated by async AppStore hydration) becomes available *after* that first render. Confirmed via direct repro: draw+save an annotation, hard-reload the markup screen (a cold navigation — exactly what a deep link, bookmark, or simple page refresh produces), and the canvas rendered **zero** SVG shapes for a **persisted, verified-present** annotation (ground-truthed via `caiq:annotations` in localStorage) — the data survived, but silently stopped rendering. Worse than a display glitch: since `save()` persists via `elementsRef.current` (`app/markup/[assetId].tsx` line ~744, `elementsToSave = elementsRef.current`), a user who reopened a markup screen this way, drew one new element, and saved, would have had that stale-empty ref **overwrite and discard every previously-saved annotation on that photo** with no warning. Fix: added a one-shot `useEffect` gated on `hydrated` (from `useAppStore()`) AND `!dirty` (never overwrite an in-progress local edit) AND a `useRef` guard (fires at most once per mount), calling the existing `applyElements(existing.elements)` helper — the same function every real edit already goes through — so both the ref and the render state resync atomically. Verified live: re-ran the exact repro after the fix — reload now shows the correct SVG shapes immediately, the element becomes selectable (selection chrome appears on tap) and draggable, and a subsequent drag+save correctly preserves the moved coordinates (confirmed via before/after localStorage comparison) instead of losing data. | Found only because the Final QA Loop explicitly tests fresh-navigation/reload flows end-to-end rather than trusting a single always-warm dev session — every earlier item's live verification happened to test *within* a session that stayed hydrated throughout, so this never surfaced until Step 5 deliberately exercised "close and reopen." Documented in full because it's the kind of defect (silent data loss on an edit-then-save) that would be very expensive to rediscover from a user bug report post-launch. |
